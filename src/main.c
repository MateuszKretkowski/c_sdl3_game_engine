#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

#include "config.h"

int main() {
    FILE *file = fopen("output.ppm", "w");
    uint8_t framebuffer[HEIGHT][WIDTH][3];

    // akcja: 
    return 0;
}